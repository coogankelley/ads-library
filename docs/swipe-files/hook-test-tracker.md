# Hook Test Tracker

Before an ad earns a spot in your full [KPI tracker](kpi-trackers.md), you use this to pick your winning hooks. You run your batch of hooks against the same body and log the engagement numbers here, so the data tells you which first three seconds actually earned attention.

The link opens a "Make a copy" prompt. Click **Make a copy** and it lands in your own Drive as yours to fill in. The shared version is view-only, so everyone starts clean.

[Make a copy of the Hook test tracker](https://docs.google.com/spreadsheets/d/11opibDetlxnYH1UqB28eldr0s96nPuPnDAJGCuPEjbA/copy)

## What It Tracks

One row per hook, in four groups:

- **Engagement:** spend, impressions, CPM, 3-second plays, Hook Rate (target above 25%), thruplays, Hold Rate (target above 15%).
- **Click:** link clicks, CTR Link (target above 3%), CPC Link (target under $1.50).
- **Webclass:** registrations (how many people that hook turned into webclass leads), Reg Rate (registrations divided by link clicks), and cost per registration.
- **Decision:** your verdict (keep or cut) and notes.

You type the inputs (spend, impressions, plays, clicks, registrations). The sheet calculates the rates and costs for you. The hooks that clear the targets are the ones you keep running. The rest you cut.

## Why the Webclass Column Matters

A click is only half the story. The hook that earns the click still has to earn the registration, and those are not always the same hook. A line can stop the scroll and pull clicks, then drop everyone before they sign up. The webclass column is what catches that, so you keep the hooks that bring real leads, not just cheap attention.

Where the registration number comes from: every ad carries a UTM tag that stamps which hook sent the click. When someone registers, your CRM records that hook on their contact. So you can pull a count per hook and enter it here, or wire your lead sheet to feed it in automatically. Either way, the point is the same: you are scoring the hook on the lead it produced, not just the click.

How hook testing works start to finish is in [Testing](../testing.md), and how to write the hooks is in [Hooks](../hooks.md).
