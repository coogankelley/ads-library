# Foundations

> **What lives here:** the ground rules before you touch Ads Manager. Show-don't-tell, where the
> ad sits in the funnel, and the three ad paths.

_Status: empty. Fill from `_intake/` material in Kelley's voice. Keep the framework intact; never paste proprietary phrasing. Flag anything that won't re-word cleanly._

## To cover
- Show, don't tell: an ad is a demonstration, not a monologue.
- The funnel context: ad → webclass → discovery → call → enrollment, and the ad's one job.
- The three ad paths and when to use each.

Draws from: framework (see [../sources.md](../sources.md)).
